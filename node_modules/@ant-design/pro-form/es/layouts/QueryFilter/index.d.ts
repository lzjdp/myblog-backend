import React from 'react';
import { FormProps } from 'antd/lib/form/Form';
import { CommonFormProps } from '../../BaseForm';
import { ActionsProps } from './Actions';
export declare type SpanConfig = number | {
    xs: number;
    sm: number;
    md: number;
    lg: number;
    xl: number;
    xxl: number;
};
export declare type BaseQueryFilterProps = Omit<ActionsProps, 'submitter' | 'setCollapsed' | 'isForm'> & {
    defaultCollapsed?: boolean;
    labelLayout?: 'default' | 'growth' | 'vertical';
    defaultColsNumber?: number;
    labelWidth?: number | 'auto';
    split?: boolean;
    /**
     * 配置列数
     */
    span?: SpanConfig;
    /**
     * 查询按钮的文本
     */
    searchText?: string;
    /**
     * 重置按钮的文本
     */
    resetText?: string;
    form?: FormProps['form'];
    /**
     * 底部操作栏的 render
     * searchConfig 基础的配置
     * props 更加详细的配置
     * {
        type?: 'form' | 'list' | 'table' | 'cardList' | undefined;
        form: FormInstance;
        submit: () => void;
        collapse: boolean;
        setCollapse: (collapse: boolean) => void;
        showCollapseButton: boolean;
     * }
     */
    optionRender?: ((searchConfig: Omit<BaseQueryFilterProps, 'submitter' | 'isForm'>, props: Omit<BaseQueryFilterProps, 'searchConfig'>, dom: React.ReactNode[]) => React.ReactNode[]) | false;
};
export declare type QueryFilterProps = FormProps & CommonFormProps & BaseQueryFilterProps & {
    onReset?: () => void;
};
declare const QueryFilter: React.FC<QueryFilterProps>;
export default QueryFilter;
